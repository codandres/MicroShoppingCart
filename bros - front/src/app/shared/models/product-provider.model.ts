import { Product } from './product.model';
import { Provider } from './provider.model';

export class ProductProvider {
    idProduct: Product;
    idProvider: Provider;
    quantity: number;
    price: number;
}